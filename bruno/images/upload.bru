meta {
  name: upload
  type: http
  seq: 2
}

post {
  url: {{url_v1}}/images/upload
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  image-upload: @file(C:\Users\tensh\Documents\GitHub\img-processing\1.jpg) @contentType(image/jpeg)
}

script:post-response {
  bru.setEnvVar("imageUUID", res.body.imageID)
}

settings {
  encodeUrl: true
  timeout: 0
}
