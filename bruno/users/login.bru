meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{url_v1}}/users/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "user",
    "password": "user"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
