meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{url_v1}}/users/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "user",
    "password": "user"
  }
}

script:post-response {
  bru.setEnvVar("accessToken", res.body.accessToken)
  
}

settings {
  encodeUrl: true
  timeout: 0
}
