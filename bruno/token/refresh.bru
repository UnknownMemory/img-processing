meta {
  name: refresh
  type: http
  seq: 1
}

get {
  url: {{url_v1}}/token/refresh
  body: json
  auth: inherit
}

script:post-response {
  bru.setEnvVar("accessToken", res.body.accessToken)
}

settings {
  encodeUrl: true
  timeout: 0
}
